plugins {
    id 'java'
}

group = 'org.example'
version = '1.0'

jar {
    manifest {
        attributes(
                'Main-Class': 'org.example.input.StartMenu'
        )
    }
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}